import { AppLayout } from '@learnsphere/ui-shared';
import { CatalogContainer } from '../catalog.container.tsx';

export const CatalogPage = () => (
	<AppLayout>
		<CatalogContainer />
	</AppLayout>
);
