import { CheckCircleOutlined, ClockCircleOutlined, PaperClipOutlined, SendOutlined, ThunderboltOutlined } from '@ant-design/icons';
import { Alert, Button, Card, Col, Form, Input, Modal, Progress, Row, Space, Tag, Typography } from 'antd';
import { useState } from 'react';
import type { MyTeamOperationsQuery } from '../generated.tsx';

const { Title, Paragraph, Text } = Typography;
type Operation = MyTeamOperationsQuery['myTeamOperations'][number];
const color = (status: string) => status === 'COMPLETED' ? 'green' : status === 'SUBMITTED' ? 'gold' : status === 'CANCELLED' ? 'default' : 'blue';

export interface OperationsProps { operations: Operation[]; learnerId: string; loading: boolean; submitting: boolean; commenting: boolean; attaching: boolean; onSubmit: (operation: Operation, note: string, evidence?: string) => void; onComment: (operation: Operation, body: string) => void; onAttach: (operation: Operation, file: File) => void; }

export const Operations = ({ operations, learnerId, loading, submitting, commenting, attaching, onSubmit, onComment, onAttach }: OperationsProps) => {
	const [selected, setSelected] = useState<Operation>();
	const [form] = Form.useForm<{ completionNote: string; completionEvidence?: string }>();
	const [commentForm] = Form.useForm<{ body: string }>();
	const assigned = operations.filter((operation) => operation.assigneeId === learnerId);
	const open = assigned.filter((operation) => !['COMPLETED', 'CANCELLED'].includes(operation.status));
	const teamGoals = operations.filter((operation) => !['COMPLETED', 'CANCELLED'].includes(operation.status));
	const thread = (operation: Operation) => <Card size="small" title={`Thread · ${operation.thread.comments.length + operation.thread.attachments.length}`} style={{ marginTop: 16 }}><Space direction="vertical" style={{ width: '100%' }} size="small">{operation.thread.comments.map((comment) => <div key={comment.id}><Text strong>{comment.authorName}</Text><Text type="secondary"> · {new Date(comment.createdAt).toLocaleString()}</Text><div>{comment.body}</div></div>)}{operation.thread.attachments.map((attachment) => <div key={attachment.id}><PaperClipOutlined /> {attachment.fileName} <Text type="secondary">({Math.ceil(attachment.size / 1024)} KB)</Text></div>)}<Form form={commentForm} layout="inline" onFinish={(values) => { onComment(operation, values.body); commentForm.resetFields(); }} style={{ width: '100%' }}><Form.Item name="body" rules={[{ required: true, min: 1 }]} style={{ flex: 1, marginBottom: 0 }}><Input placeholder="Add a comment…" /></Form.Item><Button htmlType="submit" icon={<SendOutlined />} loading={commenting}>Comment</Button><label style={{ cursor: 'pointer', opacity: attaching ? 0.6 : 1 }}><span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, border: '1px solid #d9d9d9', borderRadius: 6, padding: '4px 10px', height: 32 }}><PaperClipOutlined />{attaching ? 'Uploading…' : 'Attach'}</span><input type="file" hidden disabled={attaching} onChange={(event) => { const file = event.target.files?.[0]; if (file) onAttach(operation, file); event.currentTarget.value = ''; }} /></label></Form></Space></Card>;
	return <>
		<div style={{ marginBottom: 30 }}><Text style={{ color: '#277f6c', fontWeight: 700, letterSpacing: '.08em', textTransform: 'uppercase', fontSize: 12 }}>Team operations</Text><Title style={{ color: '#173b33', margin: '7px 0' }}>Team operations</Title><Paragraph style={{ color: '#687670', fontSize: 16 }}>Showcase the team-wide goals your group is driving and the work assigned to you. Submit your part for manager confirmation when it is ready.</Paragraph></div>
		<Alert type="info" showIcon message="Manager confirmation required" description="Submitting your work does not mark it complete. A manager must review and confirm it." style={{ marginBottom: 22 }} />
		<Title level={3} style={{ color: '#173b33', marginTop: 0 }}>Team goals</Title>
		<Row gutter={[20, 20]} style={{ marginBottom: 30 }}>{teamGoals.map((operation) => <Col key={`goal-${operation.id}`} xs={24} md={12} xl={8}><Card loading={loading} style={{ height: '100%' }} title={<Space><ThunderboltOutlined /><Tag color={color(operation.status)}>{operation.status.replaceAll('_', ' ')}</Tag></Space>}><Title level={4}>{operation.title}</Title><Text type="secondary">{operation.category} · {operation.priority.toLowerCase()}</Text><Paragraph style={{ marginTop: 14 }}>{operation.description}</Paragraph><Space direction="vertical"><Text type="secondary">Owner: {operation.assigneeDisplayName}</Text><Text type="secondary"><ClockCircleOutlined /> {operation.dueAt ? `Due ${new Date(operation.dueAt).toLocaleDateString()}` : 'No due date'}</Text></Space>{thread(operation)}</Card></Col>)}</Row>
		<Title level={3} style={{ color: '#173b33', marginTop: 0 }}>Assigned to me</Title>
		<Row gutter={[20, 20]}>{open.map((operation) => <Col key={operation.id} xs={24} md={12} xl={8}><Card loading={loading} style={{ height: '100%' }} title={<Space><ThunderboltOutlined /><Tag color={color(operation.status)}>{operation.status.replaceAll('_', ' ')}</Tag></Space>}><Title level={4}>{operation.title}</Title><Text type="secondary">Team goal · {operation.category} · {operation.priority.toLowerCase()}</Text><Paragraph style={{ marginTop: 14 }}>{operation.description}</Paragraph><Progress percent={operation.status === 'SUBMITTED' ? 100 : operation.status === 'IN_PROGRESS' ? 50 : 0} steps={2} showInfo={false} strokeColor="#277f6c" /><Space direction="vertical" style={{ width: '100%', marginTop: 14 }}><Text type="secondary"><ClockCircleOutlined /> {operation.dueAt ? `Due ${new Date(operation.dueAt).toLocaleDateString()}` : 'No due date'}</Text><Text type="secondary">Assigned by {operation.createdBy}</Text>{operation.status === 'SUBMITTED' ? <Alert type="success" message="Awaiting manager confirmation" /> : <Button type="primary" block onClick={() => { setSelected(operation); form.resetFields(); }}>Submit for confirmation</Button>}</Space></Card></Col>)}</Row>
		{operations.filter((operation) => ['COMPLETED', 'CANCELLED'].includes(operation.status)).length > 0 && <Card title="Team operations history" style={{ marginTop: 28 }}>{operations.filter((operation) => ['COMPLETED', 'CANCELLED'].includes(operation.status)).map((operation) => <div key={operation.id} style={{ display: 'flex', justifyContent: 'space-between', padding: '14px 0', borderBottom: '1px solid #edf0ee' }}><Space><CheckCircleOutlined style={{ color: operation.status === 'COMPLETED' ? '#277f6c' : '#9aa7a2' }} /><span>{operation.title}</span></Space><Tag color={color(operation.status)}>{operation.status}</Tag></div>)}</Card>}
		{!loading && operations.length === 0 && <Card><Text type="secondary">No team operations are currently assigned to you.</Text></Card>}
		<Modal title={`Submit operation · ${selected?.title ?? ''}`} open={Boolean(selected)} onCancel={() => setSelected(undefined)} onOk={() => form.submit()} confirmLoading={submitting} okText="Submit for confirmation"><Form form={form} layout="vertical" onFinish={(values) => { if (selected) onSubmit(selected, values.completionNote, values.completionEvidence); setSelected(undefined); form.resetFields(); }}><Form.Item name="completionNote" label="What did you complete?" rules={[{ required: true, min: 10 }]}><Input.TextArea rows={5} placeholder="Summarize the outcome and what you verified." /></Form.Item><Form.Item name="completionEvidence" label="Evidence link (optional)"><Input placeholder="https://…" /></Form.Item></Form></Modal>
	</>;
};
