import { MongooseSeedwork } from '@cellix/mongoose-seedwork'; import type { Organization as OrganizationDocument } from '@learnsphere/data-sources-mongoose-models'; import { Domain } from '@learnsphere/domain'; import type { OrganizationDomainAdapter } from './organization.domain-adapter.ts';
export class OrganizationRepository extends MongooseSeedwork.MongoRepositoryBase<OrganizationDocument, OrganizationDomainAdapter, Domain.Passport, Domain.Contexts.Organization.Organization<OrganizationDomainAdapter>> implements Domain.Contexts.Organization.OrganizationRepository<OrganizationDomainAdapter> {
	async getByExternalId(externalId: string) { const document = await this.model.findOne({ externalId }).exec(); if (!document) throw new Error(`Organization ${externalId} not found`); return this.typeConverter.toDomain(document, this.passport); }
	getNewInstance(input: { externalId: string; name: string; parentOrganizationId: string | null; ancestorOrganizationIds: string[]; createdBy: string }) { const adapter = this.typeConverter.toAdapter(new this.model()); return Promise.resolve(Domain.Contexts.Organization.Organization.getNewInstance(adapter, input, this.passport)); }
}
