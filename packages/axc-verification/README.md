# AXC Verification Packages

This workspace is reserved for AgentCourses harness validation:

- `acceptance-api` for Serenity/Cucumber API acceptance tests and reports.
- `archunit-tests` for architecture-boundary tests.

The packages are intentionally lightweight so future scenarios can be added
without first reshaping the repository.
