# ArchUnit Verification

Reserved for architecture-boundary tests that protect the AXC layering rules:
domain stays independent, REST delegates to application services, and
composition remains in `apps/api`.

No product-specific rules are implemented in this base branch.
