import type React from 'react';
import { SectionLayout } from './section-layout.tsx';

/** The root route of the Simnova portal — the blank landing page. */
export const Root: React.FC = () => {
	return <SectionLayout />;
};
