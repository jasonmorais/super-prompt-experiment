{
	"name": "@cellix/api-services-spec",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"files": [
		"dist"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"lint": "biome lint",
		"format": "biome format --write",
		"format:check": "biome format .",
		"prebuild": "pnpm run lint",
		"build": "tsgo --build",
		"watch": "tsgo --watch",
		"clean": "rimraf dist"
	},
	"devDependencies": {
		"@cellix/config-typescript": "workspace:*",
		"typescript": "catalog:",
		"rimraf": "catalog:"
	}
}
